begin:
    alloc(w, 2)
    store(1, w)
    store(2, w + 1)
    load(x, w)
    br(x = 1, w.true, w.false)
    
w.true:
    load(y0, w)
    jmp(end)

w.false:
    load(y1, w + 1)

end:
    phi(y, y0: w.true, y1: w.false)
    out(y)
